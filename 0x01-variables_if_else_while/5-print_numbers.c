#include <stdio.h>
/**
 * main - prints all single digit number
 * Description: This program prints all 10 base single digit numbers
 * Return: retunr 0
 */
int main(void)
{
	for (int i = 0; i < 10; i++)
		printf(i);
	return (0);
}

